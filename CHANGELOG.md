# Changelog

所有重要变更均记录于此，格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.3.6] - 2026-09-14

### 新增

- **区间摘要支持任意起止**：长度不限、可与其它区间重叠，视图默认仍按每 10 章一块提示未建的区间（被已有区间覆盖的块不再重复提示）。新增「新建区间摘要」（起止各输入一次）与区间节点的「修改区间」（改起止 = 文件改名 + 重写标题与「章节范围」，摘要正文保留）；agent 侧新增 `xReader_manageInterval`（create / editRange / delete）。
- **卷摘要**：每卷一份 `卷摘要/<卷目录名>.md`（默认卷为 `第一卷.md`），内含「章节范围 / 摘要 / 计划」三节；摘要视图新增「卷摘要」分组（与区间摘要、章节摘要并列），点击即建，分卷重命名 / 删除时自动同步，卷内任一章更新即标 ⚠ 待维护。
- **计划摘要（摘要可先于目标存在，章节 / 区间 / 卷一致）**：尚无摘要的层级都可以先写计划——章级写 `序号-标题`（界面「新建章节计划」，agent 用 `xReader_manageChapter` 的 action=insert 加 `plan=true`），卷级写未来卷名（界面「新建卷摘要」），区间级写越出现存章节的起止（`xReader_manageInterval` 的 create，或直接给 `xReader_readSummary` 一个不存在的区间）；摘要视图统一用 ✎ 标记。正文（分卷、乃至区间内的章节）写好时该摘要自动接手：移到规范名下、补齐标题与原文链接，并转为 ⚠ 提示按正文核对。
- **章节计划可插到存量中间**：把计划落在已有章节之间的序号上时，该序号及其后的正文与计划一并顺延 +1（与插章同一套逻辑，事后重写全书导航），界面在创建前会说明顺延影响、工具返回时报告顺延章数；占位的计划在正文按此位置创建（或插章）时被自动接手。
- `xReader_readSummary` 新增 scope=volume（读卷摘要，省略卷名则列出全部卷与状态）与 scope=plan（列出全部计划摘要，含区间计划），供 agent 看「接下来要写什么」。

### 变更

- **摘要删除只对计划开放**：摘要视图里只有计划摘要（章节计划、区间计划，以及卷内还有计划章节 / 还没有正文的卷摘要）才有删除入口；有正文对应的摘要不再单独删除，只随正文清理（删章节连摘要镜像删、删分卷连镜像目录与卷摘要删）。agent 侧新增 `xReader_manageChapter` / `xReader_manageVolume` 的 `action=deletePlan`，`xReader_manageInterval` 的 delete 同样只接受计划区间（已覆盖正文的区间请用 `editRange` 调整起止）。
- **卷摘要不再包含计划**：模板改为「章节范围 + 摘要」两节，计划内容统一写在章节计划与区间计划里；卷摘要状态也随之改为——卷内还有「有摘要无正文」的计划章节时标 ✎ 计划，否则只按修改时间判断 ✓ 最新 / ⚠ 待维护（取消原先「卷内没有章节即为计划」的判定，分卷尚未创建时同样只看修改时间）。

### 优化

- 摘要视图顶层改为 卷摘要 / 区间摘要 / 章节摘要 三组（由粗到细），分组与分卷 / 区间描述追加「· N 项计划」，区间节点的悬浮提示说明计划原因；无章节的书只要写了计划摘要就不再显示空态。
- 插章顺延章节序号时，同一顺延区间内「有摘要无正文」的计划摘要一并 +1，计划不会与正文错位。

### 修复

- 卷内有计划章节但卷摘要文件尚未创建时，状态不再误报为「计划」（显示为未建），`xReader_readSummary` 也不会再尝试读取不存在的卷摘要文件。
- 重命名分卷时同步迁移 `版本/<卷>/`（此前章节的备选版本留在旧卷名下、在章节目录视图里显示为 0 个版本），删除分卷时一并清理 `版本/<卷>/`（此前残留孤儿目录）。
- 重命名分卷时若目标卷名已有章节摘要镜像目录（例如已为未来卷写了计划），现在会明确报错要求先处理——此前镜像目录改名失败被静默忽略，已写好的章节摘要会留在旧卷名下变成「计划」、正文则显示摘要未建。
- 章节目录的「移动到分卷」与摘要视图的「新建章节计划」不再把章节根目录（虚拟卷）当作可选分卷，与工具侧 `requireVolumeDir` 的判定一致：书里没有其它真实分卷时移动会提示「没有可移动到的其它分卷」，没有真实分卷时新建计划直接建在根目录、不再多问一次。
- **按计划写正文**：摘要视图里 ✎ 计划章节节点新增「按计划写正文」——在该序号就地落成正文（序号被占用时其后的正文与计划顺延 +1，也允许留出空洞），计划摘要自动接手；agent 侧对应 `xReader_manageChapter` 的 insert 新增 `seq` 参数（直接指定章节序号写正文）。
- 新建章节时可以选择位置：此前只能接在全书末尾，现在填完标题后先问位置——接在全书末尾（默认）/ 插在某章之前 / 插在某章之后。插到中间时新章节归入参照章节所在分卷，其后章节序号自动顺延（结果里提示顺延了几章），插入后直接打开新章节。
- 重命名分卷时若目标卷名已有卷摘要（例如「未来卷」的计划），现在会明确报错要求先处理，不再静默覆盖。
- 删除重复入口：`ensureChapterPlan` 与 `createChapterPlan` 语义重叠，前者绕过「序号被占用则顺延」的规则、可能造出正文与计划同序号，已移除（`createChapterPlan` 位置三态统一为 参照章 / 序号 / 末尾）。
- 摘要文件（章节 / 区间 / 卷）新建时补一次 git 快照，与「写操作统一提交」的约定一致。
- `plan` 参数只对 `xReader_manageChapter` 的 insert 有效，用在别的 action 上会明确报错，不再静默按普通章节处理。
- **未配置书库目录时书架不再显示空节点**：此前书架会渲染出一个「默认」书架节点（0 本书），把欢迎页挡住，书架里因此看不到「选择书库目录 / 导入小说 / 新建书籍」入口。
- **切换书库目录后旧库的书不再算当前书**：视图标题、状态栏与 agent 工具此前会继续指向旧库（甚至已删除）的目录；现在按「书是库根的直接子目录」判定归属，切回原库即自动恢复。
- **区间摘要的「计划」判定改为按区间内序号是否齐全**：范围内还有没写的章节即计划——此前只比较区间两端与全书最小 / 最大序号，序号有空洞的书（如 1–3 与 500 之间）在两端之间建区间摘要会被误判为「最新」，且这种计划摘要无法删除。
- **计划摘要引用带 `章节摘要/` 前缀时不再嵌套出摘要目录**：把 `xReader_readSummary`(scope=plan) 输出的引用（如 `章节摘要/第一卷/0004-抵达`）再传给按计划读写正文的操作时，此前会算成 `章节摘要/章节摘要/…` 并新建目录，与删除计划时的解析不一致。
- **章节标题含文件名非法字符时摘要标题能正确同步**：标题含 `/` `:` 等字符时文件名是清洗版，摘要首行此前按原文标题匹配，一旦失配（重命名 / 序号顺延 / 计划被正文接手）摘要标题就永久滞留旧标题；现在按首行 H1 行锚重写。

## [1.3.2] - 2026-09-12

### 新增

- 书架视图标题栏新增「搜索书籍」按钮：输入关键词即按书名过滤，选中后直接打开（书多时不必在树里翻）。
- 书架视图的「更多操作」新增「导出小说为 txt」：选一本书并指定保存位置后导出为单个 txt 文本，按阅读顺序拼接全部章节正文（Markdown 标记与章节导航链接已去掉），多卷的书在每卷前补卷名。
- 世界书 / 角色卡 / 笔记三个视图与章节目录对齐：
  - 标题栏补上「新建世界书分类 / 新建角色卡分类 / 新建笔记分类」按钮（图标与分类节点一致），建在条目根目录下、支持 `地理/城邦` 这类多级路径；分类节点右键的「新建子分类 / 重命名 / 删除」保持不变，创建逻辑三处共用。
  - 条目右键新增「移动到分类」（与章节目录的「移动到分卷」同组）：目标可选同根目录下的任意分类或根目录，目标分类不存在时按路径创建；移动笔记时其关联章节链接的相对前缀按新层级自动重算，条目在磁盘上原地移动（打开的页签跟随）。
  - agent 侧同样支持条目的分类移动：`xReader_manageEntry`（action=move）按 `name` + `category` 定位条目，`targetCategory` 为目标分类（省略即根目录，不存在则创建），与章节目录的 `xReader_manageChapter`（action=move）对等。
- 子书架支持任意层级的分类（如 `题材/同人/XXX`，可继续加深）：书架视图改为多级可折叠树，子书架节点右键「新建子书架」即往下一层挂，也可在新建时直接输入 `题材/同人` 这样的多级路径（缺的上级自动补出）。分类节点显示子树内去重后的书数，重命名只改末级名并同步其下子书架路径，删除会连同其下全部子书架（只解除书籍链接，书不会被删除）。

### 变更

- **语言模型工具合并为 13 个（原 43 个）**：同族操作不再逐个注册——条目相关工具用 `kind` 参数区分类型（world=世界书 / character=角色卡 / note=笔记），写操作再用 `action` 参数区分动作，能力一一对应，无删减。
  - 条目与分类：原先三处各 7 个（list / create / rename / move / delete 条目 + 分类 rename / delete）共 21 个工具，合并为 `xReader_listEntries` / `xReader_manageEntry` / `xReader_manageCategory`；其中新增用 `action=create` 直接建分类（此前只能新建条目时隐式建出分类）。分类引用（`category` / `targetCategory`）支持完整路径或唯一的末级名，命中已有分类时自动使用其规范路径。
  - 书 / 分卷 / 章节 / 章节版本：合并为 `xReader_manageBook` / `xReader_manageVolume` / `xReader_manageChapter`（create / insert / rename / move / delete）/ `xReader_manageChapterVersion`（list / create / setPrimary / delete，原 `xReader_listChapterVersions` 并入 action=list）。
  - 摘要：`xReader_readChapterSummary` / `xReader_readIntervalSummary` 合并为 `xReader_readSummary`（scope=chapter|interval）。
  - `xReader_getCurrentChapter` / `listBooks` / `listVolumes` / `listChapters` / `setProgress` 保持不变；章节与条目的定位规则（相对路径 / 文件名 / 标题）不变。

### 优化

- 条目与分类的引用解析统一：`category` / `targetCategory` 既可用完整路径也可用唯一的末级名，命中已有分类时自动取其规范路径（此前按原文当路径用，传末级名会定位不到条目、或把条目建到别处）；服务层对分类路径、条目文件名、章节版本名与分卷目录名统一清洗校验，含 `..` 或分隔符的输入不会越出条目 / 章节目录。
- 分卷的重命名与删除按「卷名或目录名」解析（文档此前已如此描述，实际只按目录名）；根目录散章所在的虚拟卷现在给出明确提示「是章节根目录而非分卷」，不再报成「分卷不存在」。
- 「新建分类」的输入框提示改为「分类路径（用 / 分隔多级，如 地理/城邦）」，与「新建子分类」一致，明确世界书 / 角色卡 / 笔记三处都支持任意层级目录。
- 工具实现收敛：三种条目的根目录、名词、找不到条目的提示与调用文案集中到 `ENTRY_KINDS` 一处配置，分类与条目的同构注册逻辑共用，新增条目类型或调整文案时不必再改多处。
- 「添加到子书架」的选择列表改为显示末级名 + 上级路径，多级分类下不再是一长串重复的全路径；新建时同样支持输入多级路径。
- 移动失败不再静默：章节目录「移动到分卷」与条目「移动到分类」在目标已有同名文件等情况下弹出失败原因（此前命令静默返回，界面看不出发生了什么）。新建类命令与移动类命令共用同一套失败提示 helper。
- 大书库（上百至上千本书、100MB 级仓库）性能：
  - 书架视图的章节计数改用新增的 `LibraryService.countChapters` / `listChapterCounts`，只列目录不再逐章读取正文首行；书架内书数超过 50 本时改为后台统计、完成后再刷新一次，展开书架立即出节点（此前每展开一次「默认」子书架都要读完库里所有章节文件的标题）。
  - 库盘扫描改为并发受限（`SCAN_CONCURRENCY = 16`）：`listBooks` 的逐本书目录探测、分卷与章节扫描、章节摘要状态判定都不再无上限并发，避免上千本书/上千章时同时打开过多文件句柄（EMFILE）并提升吞吐。
  - 书库变更通知合并为 80ms 窗口内一次（`scheduleRefresh`）：批量导入上千本书时不再逐本触发全部视图的全量重扫（原为 O(n²) 次目录扫描 + 上千次树重渲染）。
  - 空态判断改用新增的 `LibraryService.hasBooks`（命中一本书即返回），不再为每次刷新扫描全部书目；书架视图同一次刷新内复用书目扫描，展开多个子书架不再各扫一遍库根。
  - 「清除 Git 历史」改为通知进度提示：大书库重建要重新入库全部文件（100MB 级可达数十秒），此前界面无反馈、看起来像卡死。
- 分角色朗读的音色配置修复两处：现在会读取角色卡分类（含多级）下的卡片，此前只读角色卡根目录，放进分类的角色卡不会被识别；frontmatter 的 `voice: xxx` 写法按文档生效，此前正则只匹配 `voiceId`，`voice` 被静默忽略。

## [1.3.0] - 2026-09-11

### 新增

- 世界书、角色卡、笔记三处的**分类**统一：三者的子目录即分类，均支持多级嵌套（如 `世界书/地理/城邦/`），视图内以可折叠的分类节点呈现，根目录条目照常直接列出（不再区分「未分类」）。分类可右键「新建子分类 / 重命名 / 删除」，删除分类会连同其下全部条目与子分类；新建条目时可选择已有分类、输入多级新分类路径（`剧情/支线`）或直接放根目录；空分类会写入 `.gitkeep` 纳入 git 快照。
- 新增 agent 工具 `xReader_renameCharacterCategory` / `xReader_deleteCharacterCategory` / `xReader_renameWorldCategory` / `xReader_deleteWorldCategory`；`xReader_createCharacter` / `xReader_createWorldEntry` 新增可选 `category` 参数（可多级路径，不存在则创建），`xReader_renameCharacter` / `xReader_deleteCharacter` / `xReader_renameWorldEntry` / `xReader_deleteWorldEntry` 新增可选 `category` 以便定位分类下的条目；三个 list 工具改为按分类分组输出（工具数 36 → 40）。

### 修复

- 修复大书库（数千章节文件）下「清除 Git 历史」误报失败：首次提交时 `git commit` 会为每个文件打印一行 `create mode`，输出超过 Node `execFile` 默认 1MB 缓冲，命令被中断后判为失败，而仓库其实已经重建成功。提交改为加 `--quiet` 抑制逐文件输出，输出缓冲上限提到 32MB 兜底；同类问题也影响快照提交（大书库的首个快照会误报「没有需要提交的变更」）。
- 清除历史失败时按真实原因分别提示（书库位于其他仓库内部 / `.git` 不是目录 / git 命令错误并附原始错误信息），不再把三种情况混成一句猜测。
- 提交信息里的时间戳改用本地时间（此前用 `toISOString()` 记的是 UTC，本地 10:23 的操作在历史里显示为 02:23）。

### 优化

- 移除书架视图标题栏的「刷新书架」按钮：书库目录下的 md/json 变更（含外部编辑）经文件 watcher 与 `LibraryService.onDidChange` 自动刷新全部视图，该按钮冗余，书架视图现在与其他六个视图一致。
- 元数据模板（`buildMetadataMarkdown`）里的 `## 写作要求` 小节更名为 `## 说明`；已有书中的旧标题不受影响（小节按 `## ` 标题通用解析）。
- 中文本地化术语统一：书库（原与「小说库」混用）、书籍条目（原与「小说」混用，导入 txt 仍称「导入小说」）、新建类操作（原「创建分卷 / 创建章节版本」与「新建分卷 / 新建章节版本」并存）、书籍名统一用《书名》包裹、删除确认统一为「确定删除…？」；「清除历史」按钮与命令统一为「清除 Git 历史」（英文源同步从 `Clear History` 改为 `Clear Git History`）。
- 设置项 `xReader.libraryPath` 的描述与书架空态链接改用「书库目录」「新建书籍」，与命令标题一致。
- 文档同步：README 的语言模型工具数量 35 → 40，视图表格与特性列表补上条目分类（支持多级）说明；AGENTS.md 补上条目分类的 API 约定与新增工具。

## [1.2.1] - 2026-09-10

### 新增

- 新增 agent 工具 `xReader_moveChapter`：把某一章移动到另一个分卷（章节目录右键「移动到分卷」的等价能力此前只存在于界面，agent 无法调用）。序号与文件名不变，同步搬运章节摘要镜像、重写全书导航、迁移阅读进度与关联笔记后提交 git 快照。

### 修复

- 标题/书名中含 `$&`、`$1` 等字符时不再被 `String.replace` 当作替换模式展开：章节改名写回内容首行、重命名书同步 `元数据.md` 的 `title`、笔记关联章节的 frontmatter 与链接、章节摘要的「原文」链接行均改为函数形式替换（此前会把 `$&` 展开成被替换掉的整行）。
- 重命名章节时若新标题清洗后与现有文件名相同（如只改了首尾空白），现在同样提交 git 快照并刷新视图，此前该分支静默跳过（内容首行标题已改但无快照记录）。
- 章节摘要「原文」链接的显示文字与笔记关联链接一样做方括号转义，标题含 `[` `]`（如「[番外] 重逢」）时摘要文件顶部链接不再被 Markdown 解析成两段。
- `xReader_deleteChapterVersion` 删除备选版本前不再直接执行，改为与其他删除类工具一致弹出确认。
- 章节目录中章节的右键图标顺序改为「重命名 / 新建章节版本 / 删除」（此前「新建章节版本」排在「删除」之后）；书架视图标题栏「快照 / 刷新 / 清除历史」原先有两项占用同一位置，顺序改为固定。
- 区间摘要节点的悬浮提示里的章节数不再硬编码中文，跟随界面语言（英文界面此前会掺进中文）。

### 优化

- 章节路径解析（书目录 / 分卷 / 文件名）原先在章节目录视图、翻章、朗读与 agent 工具里各写一份，现统一为 `parseChapterFilePath`，库路径本身含「章节」同名目录时的判定也不再各处不同。
- 语言模型工具 `xReader_listChapters` 与 `xReader_getCurrentChapter` 复用同一次分卷扫描读取摘要状态，不再重复遍历章节目录。
- 「新建章节」与「插入章节」共用了同一段落盘流程（建目录 → 写模板 → 接前后导航），插入章节的序号顺延逻辑不变。
- 语言模型工具清单改按 书 → 卷 → 章 → 笔记 → 角色卡 → 世界书 分组排列，同一组内统一为「列出 / 读取 → 新建 → 重命名 / 切换 → 删除」，与文档约定一致（此前书与卷的工具分散在清单各处）。
- README 勘误与补全：视图数量（六 → 七，补上「元数据」）、语言模型工具数量（30 → 35），并补上子书架、章节多版本、摘要待维护标记等说明。

## [1.2.0] - 2026-09-10

### 新增

- 书架视图分级为子书架：内置「默认」子书架始终收录全部书；用户可自建任意数量的子书架，向其中添加的书是文件夹名链接而非复制。书改名/删除时所有子书架里的链接自动级联同步。新增「新建子书架」（视图标题 ⋯ 菜单）、子书架右键「重命名 / 添加书到子书架 / 删除」（删除子书架不影响书本身）、默认书架书右键「添加到子书架…」（可选已有子书架或当场新建）；子书架节点显示收录书数。书架清单存于库根 `书架.json`，纳入 git 快照。
- 章节多版本：`版本/<卷>/<章节文件名去 .md>/<版本名>.md` 存放章节的备选版本（如第十章写好几个草稿），主版本始终是 `章节/` 下那个文件，阅读顺序、相邻章导航、章节/区间摘要、进度与笔记关联只认主版本。章节目录视图中含版本的章节显示版本数与展开箭头，展开即列出版本；章节右键「新建章节版本」（复制主版本当前内容，重名自动加序号，创建后直接打开），版本右键「设为主版本」（原主版本自动存为版本「原版」；内容原地替换，路径不变，导航/进度/笔记关联零迁移，摘要自动转待维护）/「重命名版本」/「删除版本」。
- 新增 agent 工具 `xReader_listChapterVersions` / `xReader_createChapterVersion` / `xReader_setPrimaryChapterVersion` / `xReader_deleteChapterVersion`；`xReader_listChapters` 对含版本的章节追加「｜版本×N」标记。
- 新书与导入书的目录骨架新增 `版本/` 目录（`.gitkeep` 占位，纳入 git 快照）。

### 优化

- 元数据模板与视图去掉 frontmatter `source`（原始文件）字段：`元数据.md` 不再记录导入来源文件名；旧书中手写的 `source` 字段不受影响，仍按原始键名显示。

## [1.1.0] - 2026-09-10

### 新增

- 朗读联动 x-audio：新增「朗读本章 / 分角色朗读本章 / 朗读选中文本」命令与章节页签、章节目录右键入口，经 `xaudio.speakText` 调用 [x-audio](https://marketplace.visualstudio.com/items?itemName=cameliorn.x-audio) 合成播放，长章节自动分块。
- 分角色朗读从本书「角色卡」读取角色音色：卡片首行 `# 角色名` + `- 音色：xxx`（或 frontmatter `voice` / `voiceId`）为角色固定音色，可选 `- 类型` 同时映射角色类型；无音色卡片时回退到章节目录向上查找 `.ttsvoices.json`。
- 章节目录右键与 agent 工具支持「重命名章节」：序号不变，同步更新文件名、章节摘要镜像、相邻章导航链接与阅读进度。
- 章节/分卷的删除、重命名自动重写受影响的导航链接（`[← 上一章]` / `[下一章 →]`），不再出现指向已删除文件的死链；删除章节后自动关闭其已打开的编辑器页签。
- 新增 agent 工具 `xReader_renameBook`：重命名书文件夹并同步元数据 title、当前选中书与阅读进度。
- 新增 agent 工具 `xReader_insertChapter`：在两章之间插入新章节（`after` / `before` 指定参照章节，新章节归入其所在分卷）。序号有空档时直接插入，无空档时该位置及其后章节序号自动顺延 +1，并同步文件名、章节摘要镜像、全书导航、笔记关联与阅读进度，最后提交一次 git 快照。
- 摘要「待维护」状态：章节在摘要写出之后又被改动时，摘要视图把该项标为 `⚠`（分卷与分组标题上显示 `· N 项待维护`），`xReader_listChapters` 标注「｜摘要待维护」，`readChapterSummary` / `readIntervalSummary` 读取时会在内容前附上过期提示；区间摘要在区间内任一章节更新后同样转为待维护。判定基于文件修改时间，无需额外记录：只要重新保存摘要即自动恢复 ✓。
- `xReader_getCurrentChapter` 现在一并返回当前书 `元数据.md` 的字段与各小节（简介、写作要求等），agent 拿上下文时不必再读一遍元数据文件。
- 图标体系统一为自绘 SVG：命令与 Language Model 工具原先用的内置 codicon（`$(edit)` / `$(trash)` / `$(book)` / `$(note)` 等 52 处）全部替换为 `resources/icons/` 下的自绘图标，新增 `book` / `bookmark` / `pencil` / `trash` / `prev` / `next` / `move` / `history` / `speak` / `speak-roles` / `speak-selection` / `insert` 十二个图标，与既有图标同一画法与线条规范；顺带去掉状态栏与「新建笔记分类」里的 codicon 字形（这两处只支持字体图标，改为不带图标）。
- 书架视图新增「清除 Git 历史」命令：二次确认后删除书库仓库的 `.git`，以当前文件状态重建仓库并生成初始提交（历史全部丢弃，工作区内容不变）。
- 书架与章节目录之间新增「元数据」视图：列出当前书 `元数据.md` 的 frontmatter 字段与 `## 简介` / `## 写作要求` 等小节，点击任意一项跳到文件对应行编辑；文件缺失时「打开书籍元数据」按模板重建（视图空态里也有入口）。内容由用户或 agent 自己维护，不从导入的 txt 解析。

### 修复

- 章节显示标题优先取文件内容首行 `# 标题`，修改章节名保存后侧边栏立即同步（无标题时回退文件名）。
- 修复跨卷导航链接的相对路径错误（旧格式 `第一卷/xxx` 按文件目录解析失效；分卷重命名/删除时会顺带修复）。
- 删除或重命名章节/分卷后，阅读进度迁移到相邻章或新路径，不再静默回到第一章。
- 手写无引号 `chapter: 路径` frontmatter 的笔记，在重命名/删除章节或分卷时会正确同步或移除关联。
- 重命名分卷改用 `workspace.fs.rename`，已打开的该卷章节页签跟随新路径，不再残留失效页签。
- 导航链接重写只匹配独立导航行，正文中手写的内联同名链接不再被误改。
- 新建/插入的章节文件此前不带底部导航（只有相邻章指向它），现在创建时即写入前后章链接。
- 删除章节时不再连带删掉前一章的「上一章」链接（此前该链接会被 `undefined` 当作移除处理）。

### 优化

- 翻章时不再重复刷新全部视图；分卷目录并行扫描，大书首次加载更快。
- 重命名/新建/删除类写操作统一在 LibraryService 层提交 git 快照（此前仅删除类与 agent 工具提交）。
- 文件名清洗规避 Windows 保留名（CON/PRN 等）与尾部点/空格；书库路径规范化为 fsPath，修复小写盘符配置下页签关闭与标题缓存失效。
- 笔记链接文字转义方括号，标题含 `[` `]` 不再破坏 Markdown 链接。
- 书架视图的当前书标记与章节目录统一：由「· 当前」文字改为相同的 `●` 标记。
- `元数据.md` 解析放宽：frontmatter 支持手写键名（含中文）与无引号值；重命名书籍时无引号的 `title` 也会同步更新。

## [0.1.5]

### 新增

- 新增「章节移动到分卷」功能；内置主题图标替换为自定义 SVG。
- 更新文档与图标样式。

## [0.1.3]

### 新增

- 为章节/笔记/摘要视图添加自定义图标，修复中文本地化问题。

## [0.1.2]

### 新增

- 使用自定义 SVG 图标替换视图树节点主题图标，移除资源中未用的根级图标。

## [0.1.0]

### 新增

- 导入/新建小说，卷章自动解析；书架、章节目录、摘要（章节/区间）、世界书、角色卡、笔记六视图。
- 阅读进度自动记录与恢复；章节改名级联同步；30 个 Copilot 语言模型工具；全链路 Git 快照；中英文本地化。
