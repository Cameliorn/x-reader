# Changelog

所有重要变更均记录于此，格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [Unreleased]

### 新增

- 朗读联动 x-audio：新增「朗读本章 / 分角色朗读本章 / 朗读选中文本」命令与章节页签、章节目录右键入口，经 `xaudio.speakText` 调用 [x-audio](https://marketplace.visualstudio.com/items?itemName=cameliorn.x-audio) 合成播放，长章节自动分块。
- 分角色朗读从本书「角色卡」读取角色音色：卡片首行 `# 角色名` + `- 音色：xxx`（或 frontmatter `voice` / `voiceId`）为角色固定音色，可选 `- 类型` 同时映射角色类型；无音色卡片时回退到章节目录向上查找 `.ttsvoices.json`。
- 章节目录右键与 agent 工具支持「重命名章节」：序号不变，同步更新文件名、章节摘要镜像、相邻章导航链接与阅读进度。
- 章节/分卷的删除、重命名自动重写受影响的导航链接（`[← 上一章]` / `[下一章 →]`），不再出现指向已删除文件的死链；删除章节后自动关闭其已打开的编辑器页签。
- 新增 agent 工具 `xReader_renameBook`：重命名书文件夹并同步元数据 title、当前选中书与阅读进度。

### 修复

- 章节显示标题优先取文件内容首行 `# 标题`，修改章节名保存后侧边栏立即同步（无标题时回退文件名）。
- 修复跨卷导航链接的相对路径错误（旧格式 `第一卷/xxx` 按文件目录解析失效；分卷重命名/删除时会顺带修复）。
- 删除或重命名章节/分卷后，阅读进度迁移到相邻章或新路径，不再静默回到第一章。
- 手写无引号 `chapter: 路径` frontmatter 的笔记，在重命名/删除章节或分卷时会正确同步或移除关联。
- 重命名分卷改用 `workspace.fs.rename`，已打开的该卷章节页签跟随新路径，不再残留失效页签。
- 导航链接重写只匹配独立导航行，正文中手写的内联同名链接不再被误改。

### 优化

- 翻章时不再重复刷新全部视图；分卷目录并行扫描，大书首次加载更快。
- 重命名/新建/删除类写操作统一在 LibraryService 层提交 git 快照（此前仅删除类与 agent 工具提交）。
- 文件名清洗规避 Windows 保留名（CON/PRN 等）与尾部点/空格；书库路径规范化为 fsPath，修复小写盘符配置下页签关闭与标题缓存失效。
- 笔记链接文字转义方括号，标题含 `[` `]` 不再破坏 Markdown 链接。

## [0.1.5]

### 新增

- 新增「章节移动到分卷」功能；内置主题图标替换为自定义 SVG。
- 更新文档与图标样式。

## [0.1.3]

### 新增

- 为章节/笔记/摘要视图添加自定义图标，修复中文本地化问题。

## [0.1.2]

### 新增

- 使用自定义 SVG 图标替换视图树节点主题图标，移除资源中未用的根级图标。

## [0.1.0]

### 新增

- 导入/新建小说，卷章自动解析；书架、章节目录、摘要（章节/区间）、世界书、角色卡、笔记六视图。
- 阅读进度自动记录与恢复；章节改名级联同步；30 个 Copilot 语言模型工具；全链路 Git 快照；中英文本地化。
